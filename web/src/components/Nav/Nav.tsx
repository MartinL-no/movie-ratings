interface NavProps {
  // Props
}

const Nav = ({}: NavProps) => {
  return (
    <div>
      <h2>{'Nav'}</h2>
      <p>{'Find me in ./web/src/components/Nav/Nav.tsx'}</p>
    </div>
  )
}

export { Nav, NavProps }
